define(["Parse"], function (Parse) {

    // Our basic Todo model has `content`, `order`, and `done` attributes.
    var GroupUser = Parse.Object.extend("GroupUser", {
    });
    return GroupUser;
});